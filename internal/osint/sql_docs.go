package osint

// SQL against OSINT tables lives as parameterized strings in this package
// (dbc.Pool / pgx). internal/db/sql/queries/osint_queries.sql documents the
// same statements for a future sqlc pass.
